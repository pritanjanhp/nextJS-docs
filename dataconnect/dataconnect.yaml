specVersion: "v1beta"
serviceId: "fetch-counter"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fetch-counter-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
